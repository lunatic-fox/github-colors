/**
 * Identity of important features.
 */
const constants = {
    BEFORE_CLICK: 'before-click',
    AFTER_CLICK: 'after-click',
    DSCP_PREFIX: 'cd',
    WRAPPER_PREFIX: 'wp'
};

Object.freeze(constants);

export default constants;